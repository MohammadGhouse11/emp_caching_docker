spring:
  datasource:
    url: jdbc:mysql://mysql:3306/employee_dto
    username: root
    password: Ghouse@123

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

  cache:
    type: redis

  redis:
    host: redis
    port: 6379
